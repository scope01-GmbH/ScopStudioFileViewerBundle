// Intentionally empty.
//
// rsbuild requires a `source.entry` file. Module federation exposes `plugin.ts` directly
// (see `exposes` in rsbuild.config.ts), so nothing here is ever imported or executed.
// Do not add imports to this file.
export {}
